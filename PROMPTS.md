# 提示詞範例

下面這些提示詞可以直接複製到任何 AI 代理裡使用。Codex 用 `$cihci-illustrations`，Claude Code 用 `/cihci-illustrations` 或直接描述任務讓它自動觸發。

## 只做配圖規劃

```text
Use $cihci-illustrations 先不要生圖。
請分析下面這篇文章哪裡值得配圖，輸出 5 張左右的 shot list。
每張圖寫清楚：
- 放在哪個段落後
- 圖的主題
- 核心意思
- 用哪種模式
- 結構型
- 比例
- CIHCI 醬在圖裡做什麼
- 建議元素
- 建議正體中文批註詞

<貼上文章>
```

## 文章正文配圖

```text
Use $cihci-illustrations 把下面這篇文章生成 4 張 CIHCI 醬荒誕正文配圖。
要求：16:9 橫版、純白背景、黑色手繪線稿、少量紅橙深藍正體中文手寫批註。
CIHCI 醬是畫面唯一的彩色物件。
每張圖只講一個核心結構，不要做 PPT 資訊圖，不要可愛卡通。

<貼上文章>
```

## 長文配圖策略

```text
Use $cihci-illustrations 給這篇長文做配圖策略。
不要平均配圖，只挑認知錨點：核心判斷、輸入輸出循環、前後對比、常見坑、承接路徑。
預設 6-8 張，先輸出 shot list，不要生成圖片。

<貼上文章>
```

## 單個觀點生成一張圖

```text
Use $cihci-illustrations 為這個觀點生成一張 16:9 正文配圖：

<貼上你的觀點一句話>

畫面要荒誕但清爽，CIHCI 醬必須承擔核心動作。
中文批註最多 5 個，短一點。
```

## 社群貼文配圖

```text
Use $cihci-illustrations 為這則實驗室貼文生成一張 1:1 配圖。
正方形容易顯得擠，留白要夠。
CIHCI 醬要在圖裡做事，不要只是站著。

<貼上貼文>
```

## 論文方法架構圖

```text
Use $cihci-illustrations 切到學術圖解模式，為下面這段 method 生成一張論文插圖。
比例 3:2（單欄）。結構型：方法架構。
模組不超過 5 個，全部手繪抖動，留白至少 35%。
CIHCI 醬要站在其中一個模組裡動手調整，不要站在架構外面指著看。

<貼上 method 段落>
```

## 實驗設計圖

```text
Use $cihci-illustrations 學術圖解模式，把這組消融實驗畫成一張圖。
比例 4:3。結構型：實驗設計。
CIHCI 醬把幾組條件一組一組放進裝置，輸出端是不同結果。
用紅色標出被拿掉的那一項。不要畫成表格，也不要畫長條圖。

<貼上實驗設定>
```

## 投影片圖解

```text
Use $cihci-illustrations 為這頁投影片生成一張 16:9 圖解。
這是 Lab meeting 用的，可以走學術圖解模式，但別畫成正式架構圖。
節點壓在 5 個以內，一眼看懂。

<貼上這頁要講的重點>
```

## 一次生一批

```text
Use $cihci-illustrations 為下面這份內容生成 5 張配圖。
每張都要換隱喻、換物件、換 CIHCI 醬的動作，不要有兩張長得像。
生完把每張圖用哪個隱喻列出來給我確認沒撞。

<貼上內容>
```

## 改圖：去掉標題

```text
Use $cihci-illustrations 幫我編輯這張圖。
去掉左上角的「流程圖」標題和底線，其他內容保持不變。
不要新增任何文字或物件。
```

## 改圖：修 CIHCI 醬走形

```text
Use $cihci-illustrations 這張圖裡的 CIHCI 醬走形了，變成乾淨的蝴蝶結，綠色重疊區也不見了。
請只重畫 CIHCI 醬，保住三個識別要件：兩條色帶交疊成 X、重疊處是綠色、三個深藍圓角方塊。
構圖、批註和其他黑色線稿完全不要動。
```

## 改圖：修簡體字

```text
Use $cihci-illustrations 這張圖的手寫批註有簡體字。
請把每個中文批註改寫成臺灣正體中文，字詞、手寫感、位置和顏色都保持原樣。
其他一律不動。
```

## 改圖：增強荒誕感

```text
Use $cihci-illustrations 這張圖方向對，但 CIHCI 醬有點像裝飾。
請保持核心意思不變，重生成一版：讓 CIHCI 醬成為真正推動結構運轉的那個角色。
畫面更怪一點，但仍然純白、清爽、少字。
```
